services:
  fastapi:
    build: ./backend
    ports: 
      - "${FAST_API_PORT}:5000"
    environment:
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
    # depends_on:
    #   redis:
    #     condition: service_healthy
  # celery:
  #   build: ./backend/
  #   ports: 
  #     - "${CELERY_WORKER_PORT}:4000"
  #   environment:
  #     - REDIS_HOST=${REDIS_HOST}
  #     - REDIS_PORT=${REDIS_PORT}
  #   depends_on:
  #     redis:
  #       condition: service_healthy
  redis:
    image: redis:alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 30s
      retries: 5
      start_period: 15s
  # searxng:
  #   image: searxng/searxng:latest
  #   ports:
  #     - "8080:8080"
  